import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
	paper: {
		padding: '20px',
		width: '18vw',
	},
		popover: {
		pointerEvents: 'none',
	},
}));