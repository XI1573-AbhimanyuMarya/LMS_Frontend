import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles(theme => ({
    avatar: {
        marginRight: '.6em',
    },
    notification: {
        marginRight: '1.1em'
    },
    typography: {
        fontWeight: 600,
        fontSize: 12
    },
    iconButton: {
        padding: '.5em'
    }
}))